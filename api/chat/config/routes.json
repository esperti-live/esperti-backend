{
  "routes": [
    {
      "method": "GET",
      "path": "/chats",
      "handler": "chat.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/chats",
      "handler": "chat.create",
      "config": {
        "policies": []
      }
    }
  ]
}
