{
  "routes": [
    {
      "method": "GET",
      "path": "/sessions",
      "handler": "session.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/sessions/count",
      "handler": "session.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/sessions",
      "handler": "session.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/sessions/:id",
      "handler": "session.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/sessions/:id",
      "handler": "session.delete",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/sessions/:slug/start",
      "handler": "session.start",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/sessions/:slug/complete",
      "handler": "session.complete",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/sessions/:slug",
      "handler": "session.isValid",
      "config": {
        "policies": []
      }
    }
  ]
}